# Synthetic examples

Files here are generated from example UUIDs and a synthetic current-policy fixture. They show output format only. They are not tenant exports, What If results, sign-in logs, or proof of enforcement.

Regenerate with the Quick start commands in the root README and replace the example transcripts if output intentionally changes.
